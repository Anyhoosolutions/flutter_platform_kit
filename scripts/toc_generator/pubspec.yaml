name: toc_generator
description: A command-line tool for generating toc.json by scanning packages and tools directories for documentation files.
version: 1.0.0
repository: https://github.com/Anyhoosolutions/flutter_platform_kit

environment:
  sdk: ^3.5.2

# No external dependencies needed - only uses dart:io and dart:convert

dev_dependencies:
  lints: ^4.0.0
  test: '>=1.25.0 <2.0.0'
