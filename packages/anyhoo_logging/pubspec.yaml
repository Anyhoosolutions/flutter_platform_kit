name: anyhoo_logging
resolution: workspace
description: "A package for logging."
version: 0.0.6
homepage: https://anyhoosolutions.web.app/documentation/flutter_platform_kit
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.4 " # Or the corresponding Flutter version

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.26.3
  bloc_test: ^10.0.0

flutter:
  generate: true
  uses-material-design: true
