name: screenshot_kit
resolution: workspace
description: Shared Flutter test surface sizing, theme, and dart-define keys for screenshots and Alchemist CI goldens.
version: 0.1.0
homepage: https://anyhoosolutions.web.app/documentation/flutter_platform_kit
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.4 " # Or the corresponding Flutter version

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
